{
	"version": 2,
	"rewrites": [
		{
			"source": "/api/(.*)",
			"destination": "/(.*)"
		}
	],
	"buildCommand": "pnpx puppeteer browsers install chrome",
	"installCommand": "pnpm i",
	"outputDirectory": "."
}